# Server Configuration
PORT=3000
NODE_ENV=development

# API Key (used for static API key authentication)
API_KEY=your-api-key-here

# Database Configuration (placeholder)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=admin
DB_PASSWORD=secret
